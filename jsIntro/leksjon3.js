// Arraymetoder

// Oppgave 1 - join() / toString(). Fullfør setning under slik at elementene fra listeMat blir lagt til i slutten av setningen.

let listeMat = ["pizza", "tiramisu", "tortelini", "risotto"]
let setning = "Jeg elsker italiensk mat, som f.eks"

setning = setning + " " + listeMat.join(", ")

console.log(setning)

// Oppgave 2
